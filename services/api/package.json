{
  "name": "flp-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "FLP platform API: webhooks, signed URLs, job state machine, scheduled reminders.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@hono/node-server": "^1.14.0",
    "@supabase/supabase-js": "^2.116.0",
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.2",
    "tsx": "^4.20.0",
    "typescript": "~5.9.0"
  },
  "engines": { "node": ">=24" }
}
